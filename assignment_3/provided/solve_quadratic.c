#include "solve_quadratic.h"
#incldue "solve_linear.h"
#include "stdio.h"

void solve_quadratic(float a, float b, float c) {
  if(a = 0) {
    solve_linear(b,c);
  } else {
  printf("solving quadratic a=%f, b=%f, c=%f",a,b,c);

  if( b*b
  }
}
